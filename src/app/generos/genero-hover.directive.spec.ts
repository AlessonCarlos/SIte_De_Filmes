import { GeneroHoverDirective } from './genero-hover.directive';

describe('GeneroHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new GeneroHoverDirective();
    expect(directive).toBeTruthy();
  });
});
