import { GeneroFormatadoPipe } from './genero-formatado.pipe';

describe('GeneroFormatadoPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneroFormatadoPipe();
    expect(pipe).toBeTruthy();
  });
});
