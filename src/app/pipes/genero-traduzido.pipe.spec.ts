import { GeneroTraduzidoPipe } from './genero-traduzido.pipe';

describe('GeneroTraduzidoPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneroTraduzidoPipe();
    expect(pipe).toBeTruthy();
  });
});
